# Dev-only deps for the pytest suite and the Playwright smoke/probe layer. Versions pinned
# to whatever pip resolved unconstrained against requirements.txt's pins on 2026-08-29
# (BUILD_PLAN_2A.md Stream A build step 1) -- recorded here rather than left floating so a
# re-install is reproducible; bump deliberately and re-run the probe.
pytest==9.1.1
playwright==1.62.0
