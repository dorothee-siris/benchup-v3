BenchUp v3 — palette validation log
Stream D0, 2026-08-29. Validator: dataviz skill scripts/validate_palette.js.
House rule (SIRIS CLAUDE.md): --mode light ONLY, dark-mode pass skipped deliberately.
Two runs. Run 1 is the one that SHIPS (lib/palette.py TYPE_COLORS + FOCAL).
Run 2 documents, by design, why COMPARISON/NEUTRAL/INK are excluded from the
categorical set (same finding class as Lorraine helpers.py hitting #333333 as
chroma-0 text ink — see RULES.md §4 validator edit 2026-08-15).

================================================================================
RUN 1 (SHIPS) — institution-type identity set: FOCAL + the 4 non-education
type-identity hues (education is deliberately uncoloured — see lib/palette.py
TYPE_COLORS docstring for the one-line justification).

$ node scripts/validate_palette.js "#0072B2,#D55E00,#009E73,#CC79A7,#6A3D9A" --mode light

Palette (light, surface #fcfcfb, categorical): 5 slots
  [PASS] Lightness band         all 5 inside L 0.43-0.77
  [PASS] Chroma floor           all 5 >= 0.1
  [WARN] CVD separation         worst adjacent #CC79A7<->#009E73 dE 7.6 (deutan) . tritan 23.5
  [PASS] Normal-vision floor    worst adjacent #6A3D9A<->#CC79A7 dE 24.1 (normal)
  [WARN] Contrast vs surface    below 3:1 - relief required (visible labels or table view): [["#CC79A7",2.98]]

  -> ALL CHECKS PASS  (CVD in the 6-8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.

Exit code: 0

Disposition of the two WARN lines (both carried as BINDING rules, not dismissed):
  - CVD separation WARN (#CC79A7<->#009E73, deutan dE 7.6, below the 8.0 comfortable
    floor): legal only with secondary encoding. lib/badges.py (Stream F) never renders
    a type badge as a bare colour fill — text + glyph always accompany it (DESIGN_TOKENS.md
    badge grammar). Assignment order in TYPE_COLORS also keeps #009E73 and #CC79A7
    non-adjacent per RULES.md §4's explicit instruction, so the two rarely sit side by
    side in a legend or chip row in the first place.
  - Contrast-vs-surface WARN (#CC79A7 = 2.98:1 on white, "binding relief"): #CC79A7
    (government+funder) never ships as a bare fill/dot without its text label —
    identical relief rule to Lorraine VIZ_SPEC.md §1.1's Social Sciences slot, same hex.

================================================================================
RUN 2 (documents an exclusion, expected FAIL) — COMPARISON grey (#8C9196),
NEUTRAL background (#E6E8EB) and text INK (#333333), run together to confirm
none of the three qualifies as a categorical identity colour. This is BY DESIGN:
RULES.md §4 calls #8C9196/#E6E8EB "neutral, not an identity" and amends #333333
as "TEXT ONLY... fails the palette validator as a series colour" — this run
reproduces that finding for BenchUp v3's exact hexes before they are locked into
lib/palette.py as FOCAL/COMPARISON/NEUTRAL/INK (never run through the categorical
CVD check; COMPARISON and INK are checked for WCAG text/UI contrast only, which
both already carry an established precedent in Lorraine's shipped app at the same
hex values).

$ node scripts/validate_palette.js "#8C9196,#E6E8EB,#333333" --mode light

Palette (light, surface #fcfcfb, categorical): 3 slots
  [FAIL] Lightness band         outside band: [["#E6E8EB",0.93],["#333333",0.321]]
  [FAIL] Chroma floor           below floor (reads gray): [["#8C9196",0.009],["#E6E8EB",0.005],["#333333",0]]
  [PASS] CVD separation         worst adjacent #E6E8EB<->#8C9196 dE 27.5 (protan) . tritan 27.7
  [PASS] Normal-vision floor    worst adjacent #E6E8EB<->#8C9196 dE 27.6 (normal)
  [WARN] Contrast vs surface    below 3:1 - relief required (visible labels or table view): [["#E6E8EB",1.2]]

  -> FAILED - fix the marked checks  (CVD in the 6-8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.

Exit code: 1

Disposition: EXPECTED and CORRECT. #8C9196/#E6E8EB/#333333 are never assigned to
lib/palette.py's TYPE_COLORS (the only categorical set in the app) — they are
FOCAL's comparison grey, the page/table neutral background, and body text ink
respectively. Their WCAG contrast is verified against their actual usage instead:
  - INK #333333 on white background: 12.6:1 (well above the 4.5:1 body-text floor;
    ui-ux-pro-max ux-guidelines.csv "Color Contrast" cites the identical pair as its
    own Do-example, "#333 on white (7:1)" — BenchUp's #333333 clears it by a wider
    margin).
  - COMPARISON #8C9196 is never used for text, only for bars/rows/reference marks
    already carrying a text label (RULES.md §6 "a legend names the measure... for
    colour, show endpoints").
  - NEUTRAL #E6E8EB is a fill only (table zebra stripe, empty-state panel, disabled
    control), never a text colour — the 1.2:1 contrast-vs-surface WARN above is
    irrelevant to that usage (it is not a mark that needs to stand out from #fcfcfb).

################################################################################
# R1 / stream R-D2 additions (2026-08-29) -- the four IDENTITY FAMILIES of
# BUILD_PLAN_2A.md L19 + the chrome tokens. Same rule as runs 1-2: `--mode
# light` ONLY (SIRIS house rule; the dark pass is skipped deliberately, not
# forgotten). Every R1 run adds `--surface "#FFFFFF"` because that is the
# surface the app actually paints (lib/palette.py SURFACE / Streamlit light
# theme), not the skill's off-white default, and `--pairs all` because these
# are bar panels and legends where any pair can end up adjacent after a sort
# toggle -- `adjacent` would under-test them.
#
# Working directory for every command below:
#   C:/Users/theod/AppData/Local/Temp/claude/bundled-skills/2.1.250/
#     7d34fd5c5ca02f9d932830007398ac3e/dataviz
# (the `dataviz` skill's own base directory; node v24.19.0)
################################################################################

================================================================================
RUN 3 (DESCRIPTIVE, hexes are FIXED) -- OA_DOMAIN_COLORS, the four OpenAlex
domains (BenchUp V2 / Lorraine DOMAIN_COLORS lineage; fields, subfields and
topics inherit these). These hexes are inherited, not chosen: the FAIL and the
WARNs below are carried as BINDING RELIEF RULES in lib/palette.py, never as a
reason to change a value.

$ node scripts/validate_palette.js "#0CA750,#FFCB3A,#8190FF,#F85C32" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 4 slots
  [FAIL] Lightness band         outside band: [["#FFCB3A",0.865]]
  [PASS] Chroma floor           all 4 >= 0.1
  [WARN] CVD separation         worst all-pairs #F85C32↔#0CA750 ΔE 7.6 (deutan) · tritan 7.3
  [PASS] Normal-vision floor    worst all-pairs #F85C32↔#FFCB3A ΔE 24.9 (normal)
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#FFCB3A",1.52],["#8190FF",2.85]]

  → FAILED — fix the marked checks  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


Exit code: 0

================================================================================
RUN 4a -- ERC candidate SCREEN. Each candidate is paired with each of the
four OA hues through the validator's own exported validate(); the table reports
the WORST normal-vision and worst CVD distance against the OA quartet, and the
WCAG contrast against the white surface. Requirement (R1 brief): normal-vision
dE >= 12 from every OA hue. Script: design-system/ab/screen_erc.mjs (kept in the
repo so the screen re-runs).

$ node "C:/Users/theod/SIRIS/Internal Projects/BenchUp/V3/app/design-system/ab/screen_erc.mjs" "#1F4E9C" "#00706B" "#A31A6E" "#8A5A00" "#B07A00" "#4B2E83" "#7D2E68" "#A10A4E" "#C24E00" "#3B4CA8" "#00566E" "#9B1B6B" "#5E5C00" "#8C4A1E" "#B5197C" "#1B4F91" "#8A2E62"
#1F4E9C  vsOA minNormal=25.8 (Physical #8190FF)  minCVD=24.5 (Physical #8190FF)  contrast=7.99
#00706B  vsOA minNormal=18.8 (Life #0CA750)  minCVD=11.6 (Health #F85C32)  contrast=5.95
#A31A6E  vsOA minNormal=24.4 (Health #F85C32)  minCVD=17.6 (Life #0CA750)  contrast=7.17
#8A5A00  vsOA minNormal=21.2 (Health #F85C32)  minCVD=10.0 (Health #F85C32)  contrast=5.93
#B07A00  vsOA minNormal=14.6 (Health #F85C32)  minCVD=2.4 (Health #F85C32)  contrast=3.73
#4B2E83  vsOA minNormal=31.2 (Physical #8190FF)  minCVD=29.8 (Health #F85C32)  contrast=10.41
#7D2E68  vsOA minNormal=29.2 (Health #F85C32)  minCVD=23.1 (Life #0CA750)  contrast=8.53
#A10A4E  vsOA minNormal=24.0 (Health #F85C32)  minCVD=17.5 (Life #0CA750)  contrast=7.87
#C24E00  vsOA minNormal=11.4 (Health #F85C32)  minCVD=7.3 (Life #0CA750)  contrast=4.79
#3B4CA8  vsOA minNormal=23.7 (Physical #8190FF)  minCVD=23.3 (Physical #8190FF)  contrast=7.54
#00566E  vsOA minNormal=27.4 (Life #0CA750)  minCVD=19.5 (Health #F85C32)  contrast=8.22
#9B1B6B  vsOA minNormal=25.7 (Health #F85C32)  minCVD=19.1 (Life #0CA750)  contrast=7.60
#5E5C00  vsOA minNormal=21.2 (Life #0CA750)  minCVD=11.9 (Health #F85C32)  contrast=6.98
#8C4A1E  vsOA minNormal=21.8 (Health #F85C32)  minCVD=14.1 (Health #F85C32)  contrast=6.75
#B5197C  vsOA minNormal=22.3 (Health #F85C32)  minCVD=14.9 (Life #0CA750)  contrast=6.19
#1B4F91  vsOA minNormal=26.8 (Physical #8190FF)  minCVD=24.3 (Health #F85C32)  contrast=8.16
#8A2E62  vsOA minNormal=26.5 (Health #F85C32)  minCVD=20.3 (Life #0CA750)  contrast=7.93

Exit code: 0

================================================================================
RUN 4 (SHIPS) -- ERC_DOMAIN_COLORS alone: PE #1F4E9C, LS #9B1B6B, SH #8A5A00.

$ node scripts/validate_palette.js "#1F4E9C,#9B1B6B,#8A5A00" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 3 slots
  [PASS] Lightness band         all 3 inside L 0.43–0.77
  [PASS] Chroma floor           all 3 >= 0.1
  [PASS] CVD separation         worst all-pairs #9B1B6B↔#1F4E9C ΔE 8.8 (protan) · tritan 9.3
  [PASS] Normal-vision floor    worst all-pairs #8A5A00↔#9B1B6B ΔE 20.3 (normal)
  [PASS] Contrast vs surface    all 3 >= 3:1

  → ALL CHECKS PASS  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


Exit code: 0

================================================================================
RUN 4b -- the two runners-up, for the record (both rejected in lib/palette.py).

$ node scripts/validate_palette.js "#1F4E9C,#A31A6E,#8A5A00" --mode light --surface "#FFFFFF" --pairs all; echo; node scripts/validate_palette.js "#1F4E9C,#B5197C,#8A5A00" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 3 slots
  [PASS] Lightness band         all 3 inside L 0.43–0.77
  [PASS] Chroma floor           all 3 >= 0.1
  [PASS] CVD separation         worst all-pairs #A31A6E↔#1F4E9C ΔE 8.1 (protan) · tritan 9.6
  [PASS] Normal-vision floor    worst all-pairs #8A5A00↔#A31A6E ΔE 20.5 (normal)
  [PASS] Contrast vs surface    all 3 >= 3:1

  → ALL CHECKS PASS  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.



Palette (light, surface #FFFFFF, categorical): 3 slots
  [PASS] Lightness band         all 3 inside L 0.43–0.77
  [PASS] Chroma floor           all 3 >= 0.1
  [FAIL] CVD separation         worst all-pairs #B5197C↔#1F4E9C ΔE 5.5 (protan) · tritan 11.1
  [PASS] Normal-vision floor    worst all-pairs #8A5A00↔#B5197C ΔE 22.2 (normal)
  [PASS] Contrast vs surface    all 3 >= 3:1

  → FAILED — fix the marked checks  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


Exit code: 0

================================================================================
RUN 5 (SHIPS -- the co-occurrence the R1 brief requires) -- the four OA
domain hues AND the three ERC hues, validated as ONE set of seven.

$ node scripts/validate_palette.js "#0CA750,#FFCB3A,#8190FF,#F85C32,#1F4E9C,#9B1B6B,#8A5A00" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 7 slots
  [FAIL] Lightness band         outside band: [["#FFCB3A",0.865]]
  [PASS] Chroma floor           all 7 >= 0.1
  [WARN] CVD separation         worst all-pairs #F85C32↔#0CA750 ΔE 7.6 (deutan) · tritan 7.3
  [PASS] Normal-vision floor    worst all-pairs #8A5A00↔#9B1B6B ΔE 20.3 (normal)
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#FFCB3A",1.52],["#8190FF",2.85]]

  → FAILED — fix the marked checks  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


Exit code: 0

================================================================================
RUN 6a (SHIPS) -- DOCTYPE_COLORS: Lorraine's four validated pass-6 hues plus
the NEW hue for `letter` (#A10A4E, the slot Lorraine's own palette carried for
conference papers; BenchUp's corpus has letters where Lorraine's had those).
Rejected for `letter`, all measured: #C24E00 (CVD 4.0 vs #667900), #1F4E9C (CVD
4.3 / normal 14.0 vs #7838B6, and it is the ERC PE hue), #8A5A00 (CVD 3.0 /
normal 10.1 vs #667900 -- two ochres in one legend).

$ node scripts/validate_palette.js "#22A2BD,#A55F8F,#667900,#7838B6,#A10A4E" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 5 slots
  [PASS] Lightness band         all 5 inside L 0.43–0.77
  [PASS] Chroma floor           all 5 >= 0.1
  [PASS] CVD separation         worst all-pairs #A55F8F↔#22A2BD ΔE 8.2 (deutan) · tritan 9.4
  [PASS] Normal-vision floor    worst all-pairs #A10A4E↔#A55F8F ΔE 15.0 (normal)
  [PASS] Contrast vs surface    all 5 >= 3:1

  → ALL CHECKS PASS  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


Exit code: 0

================================================================================
RUN 6b (DOCUMENTS A BOUNDED EXCLUSION, expected FAIL) -- the five document
types AND the four OA domains as one nine-slot set. The two failing pairs both
PRE-EXIST this stream and neither involves the new hue: #667900 (book) <->
#0CA750 (Life) at normal-vision 13.1, and #667900 <-> #F85C32 (Health) at protan
3.3. DISPOSITION: the COEXISTENCE RULE (lib/palette.py module docstring) -- the
yearly-breakdown pair shows EITHER the domain series OR the document-type series,
chosen by one segmented control, with the chip legend rebuilt on each swap, so
the two families are never in one figure and never in one legend. Recorded rather
than suppressed because the swap does put them in SEQUENTIAL memory; the fix if
that ever becomes a real complaint is to re-hue `book`, not to give the doctype
family a second chart-dependent palette.

$ node scripts/validate_palette.js "#22A2BD,#A55F8F,#667900,#7838B6,#A10A4E,#0CA750,#FFCB3A,#8190FF,#F85C32" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 9 slots
  [FAIL] Lightness band         outside band: [["#FFCB3A",0.865]]
  [PASS] Chroma floor           all 9 >= 0.1
  [FAIL] CVD separation         worst all-pairs #F85C32↔#667900 ΔE 3.3 (protan) · tritan 3.6
  [FAIL] Normal-vision floor    worst all-pairs #0CA750↔#667900 ΔE 13.1 (normal) — below 15, hard to tell apart even with full color vision
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#FFCB3A",1.52],["#8190FF",2.85]]

  → FAILED — fix the marked checks  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


Exit code: 0

================================================================================
RUN 7 (DESCRIPTIVE ONLY -- the UN fixes these) -- SDG_COLORS, all seventeen
official goal colours. SOURCE: manager-supplied, matching the 2019 UN guidelines
as commonly published. A live check of https://www.un.org/sustainabledevelopment/
news/communications-material/ on 2026-08-29 CONFIRMS the governing document --
'Sustainable Development Goals Guidelines for the use of the SDG logo including
the colour wheel and 17 icons', the August 2019 edition (SDG_Guidelines_AUG_2019_
Final.pdf), since revised September 2023 -- but that page publishes the
downloadable assets, not the hex table, so the values are recorded with that
provenance rather than quoted from the PDF.
A FAIL here is NEVER a reason to change a hex. Relief is structural instead
(VIZ_SPEC section 2.11): the SDG panel is a labelled bar chart in fixed goal
order, every bar carrying its goal number and short label on the axis, so colour
is brand recognition and identity is never colour-alone. Note in particular the
worst pair, #FD9D24 (goal 11) <-> #DDA63A (goal 2) at normal-vision 5.3: the UN
palette simply contains two near-identical ambers, and no chart of ours may rely
on telling them apart by hue. Goal 17 (#19486A) is STORED BUT UNUSED -- the
classifier does not cover it -- which is why the second run below, the sixteen
the app actually draws, is the one that describes what a reader sees.

$ node scripts/validate_palette.js "#E5243B,#DDA63A,#4C9F38,#C5192D,#FF3A21,#26BDE2,#FCC30B,#A21942,#FD6925,#DD1367,#FD9D24,#BF8B2E,#3F7E44,#0A97D9,#56C02B,#00689D,#19486A" --mode light --surface "#FFFFFF" --pairs all; echo; echo "--- the SIXTEEN actually drawn (goal 17 dropped), ADJACENT pairs = the fixed goal display order ---"; node scripts/validate_palette.js "#E5243B,#DDA63A,#4C9F38,#C5192D,#FF3A21,#26BDE2,#FCC30B,#A21942,#FD6925,#DD1367,#FD9D24,#BF8B2E,#3F7E44,#0A97D9,#56C02B,#00689D" --mode light --surface "#FFFFFF"

Palette (light, surface #FFFFFF, categorical): 17 slots
  [FAIL] Lightness band         outside band: [["#FCC30B",0.845],["#FD9D24",0.777],["#19486A",0.387]]
  [FAIL] Chroma floor           below floor (reads gray): [["#19486A",0.077]]
  [FAIL] CVD separation         worst all-pairs #FD9D24↔#DDA63A ΔE 1.3 (protan) · tritan 0.9
  [FAIL] Normal-vision floor    worst all-pairs #FD9D24↔#DDA63A ΔE 5.3 (normal) — below 15, hard to tell apart even with full color vision
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#DDA63A",2.19],["#26BDE2",2.22],["#FCC30B",1.62],["#FD6925",2.91],["#FD9D24",2.09],["#56C02B",2.34]]

  → FAILED — fix the marked checks  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


--- the SIXTEEN actually drawn (goal 17 dropped), ADJACENT pairs = the fixed goal display order ---

Palette (light, surface #FFFFFF, categorical): 16 slots
  [FAIL] Lightness band         outside band: [["#FCC30B",0.845],["#FD9D24",0.777]]
  [PASS] Chroma floor           all 16 >= 0.1
  [WARN] CVD separation         worst adjacent #4C9F38↔#DDA63A ΔE 7.3 (protan) · tritan 6.4
  [FAIL] Normal-vision floor    worst adjacent #BF8B2E↔#FD9D24 ΔE 11.8 (normal) — below 15, hard to tell apart even with full color vision
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#DDA63A",2.19],["#26BDE2",2.22],["#FCC30B",1.62],["#FD6925",2.91],["#FD9D24",2.09],["#56C02B",2.34]]

  → FAILED — fix the marked checks  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


Exit code: 0

================================================================================
RUN 8 (documents an exclusion, expected FAIL) -- the CHROME tokens
INK_SECONDARY #5A5F66, BORDER #E3E6EA, GRID #D9DDE2, run together to confirm that
none of the three qualifies as a categorical identity colour. Same class as run 2
(COMPARISON / NEUTRAL / INK). DISPOSITION: EXPECTED AND CORRECT -- these are
secondary text, a hairline and a gridline; they are checked against their real
usage instead. INK_SECONDARY #5A5F66 on #FFFFFF measures 6.43:1 (see the contrast table
printed after the run below), above the 4.5:1
body-text floor, which is the only check that matters for the KPI-tile sublines,
gutter numbers and chip labels it paints. BORDER and GRID are furniture that must
RECEDE (dataviz: 'recessive grid/axes'), so a low contrast is the requirement,
not the defect -- neither is ever a mark and neither ever carries text.

$ node scripts/validate_palette.js "#5A5F66,#E3E6EA,#D9DDE2" --mode light --surface "#FFFFFF" --pairs all; echo; node -e "import(String.raw\`file:///C:/Users/theod/AppData/Local/Temp/claude/bundled-skills/2.1.250/7d34fd5c5ca02f9d932830007398ac3e/dataviz/scripts/validate_palette.js\`).then(m=>{for(const c of [\"#5A5F66\",\"#E3E6EA\",\"#D9DDE2\",\"#333333\",\"#8C9196\"]) console.log(c, \"contrast vs #FFFFFF =\", m.contrast(c,\"#FFFFFF\").toFixed(2)+\":1\")})"

Palette (light, surface #FFFFFF, categorical): 3 slots
  [FAIL] Lightness band         outside band: [["#E3E6EA",0.924],["#D9DDE2",0.896]]
  [FAIL] Chroma floor           below floor (reads gray): [["#5A5F66",0.013],["#E3E6EA",0.006],["#D9DDE2",0.008]]
  [FAIL] CVD separation         worst all-pairs #D9DDE2↔#E3E6EA ΔE 2.7 (protan) · tritan 2.8
  [FAIL] Normal-vision floor    worst all-pairs #D9DDE2↔#E3E6EA ΔE 2.8 (normal) — below 15, hard to tell apart even with full color vision
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#E3E6EA",1.25],["#D9DDE2",1.36]]

  → FAILED — fix the marked checks  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


#5A5F66 contrast vs #FFFFFF = 6.43:1
#E3E6EA contrast vs #FFFFFF = 1.25:1
#D9DDE2 contrast vs #FFFFFF = 1.36:1
#333333 contrast vs #FFFFFF = 12.63:1
#8C9196 contrast vs #FFFFFF = 3.18:1

Exit code: 0

================================================================================
PHASE 2B, STREAM V (2026-08-29) -- the FIFTH identity family
(INSTITUTION_COLORS, Compare/Collaborate only) and the ONE ordinal ramp
(GREY_STATE_COLORS, the coverage strip). Same discipline as runs 1-8: every
hex below was RUN, none was eyeballed, and a FAIL is recorded with its
disposition rather than made to disappear by changing a colour.

Two screening tools were written for this pass and are kept beside the runner
scripts, `design-system/ab/screen_inst.mjs` (full pairwise matrix + distance to
the FOCAL/COMPARISON chrome) and `design-system/ab/screen_ordinal.mjs` (the
skill's own `validateOrdinal`, which is the applicable validator for a
sequential ramp). Both import the dataviz skill's `validate_palette.js`
directly, exactly as `screen_erc.mjs` did in R1.

================================================================================
RUN 9 -- INSTITUTION_COLORS, the six in SHIPPED SLOT ORDER, all pairs.
RESULT: ALL CHECKS PASS. The two WARN lines are carried as BINDING RELIEF and
are written into lib/palette.py and VIZ_SPEC section 2 ter:
  * #CC79A7 <-> #009E73 deutan 7.6 is inside the 6-8 floor band -> "legal ONLY
    with secondary encoding". Every Compare view supplies three: the category
    name on the y axis, the institution chip legend above the figure, and the
    per-mark hover that names the institution in words.
  * #56B4E9 2.31:1 and #E69F00 2.25:1 vs SURFACE -> "relief required (visible
    labels or table view)", not dismissable. Satisfied by the same labels plus
    the CSV and xlsx export of every panel (2B-13).

$ node scripts/validate_palette.js "#D55E00,#009E73,#6A3D9A,#CC79A7,#56B4E9,#E69F00" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 6 slots
  [PASS] Lightness band         all 6 inside L 0.43–0.77
  [PASS] Chroma floor           all 6 >= 0.1
  [WARN] CVD separation         worst all-pairs #CC79A7↔#009E73 ΔE 7.6 (deutan) · tritan 8.5
  [PASS] Normal-vision floor    worst all-pairs #E69F00↔#D55E00 ΔE 15.6 (normal)
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#56B4E9",2.31],["#E69F00",2.25]]

  → ALL CHECKS PASS  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.

================================================================================
RUN 9b -- the k = 2 / 3 / 4 / 5 PREFIXES, i.e. the palettes actually drawn
when the basket holds fewer than six institutions. This is the run that
justifies the SLOT ORDER: the two weakest pairs and the two sub-3:1 contrasts
all sit in the last three slots, so k <= 3 draws a completely warning-free
palette and Collaborate (exactly two institutions, 2B-7) never sees a warning
at all.

# (the k=2, k=3, k=4, k=5 prefixes actually drawn when the basket is smaller)
$ node scripts/validate_palette.js "#D55E00,#009E73" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 2 slots
  [PASS] Lightness band         all 2 inside L 0.43–0.77
  [PASS] Chroma floor           all 2 >= 0.1
  [PASS] CVD separation         worst all-pairs #009E73↔#D55E00 ΔE 11.0 (deutan) · tritan 31.4
  [PASS] Normal-vision floor    worst all-pairs #009E73↔#D55E00 ΔE 25.8 (normal)
  [PASS] Contrast vs surface    all 2 >= 3:1

  → ALL CHECKS PASS  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


$ node scripts/validate_palette.js "#D55E00,#009E73,#6A3D9A" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 3 slots
  [PASS] Lightness band         all 3 inside L 0.43–0.77
  [PASS] Chroma floor           all 3 >= 0.1
  [PASS] CVD separation         worst all-pairs #009E73↔#D55E00 ΔE 11.0 (deutan) · tritan 22.5
  [PASS] Normal-vision floor    worst all-pairs #009E73↔#D55E00 ΔE 25.8 (normal)
  [PASS] Contrast vs surface    all 3 >= 3:1

  → ALL CHECKS PASS  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


$ node scripts/validate_palette.js "#D55E00,#009E73,#6A3D9A,#CC79A7" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 4 slots
  [PASS] Lightness band         all 4 inside L 0.43–0.77
  [PASS] Chroma floor           all 4 >= 0.1
  [WARN] CVD separation         worst all-pairs #CC79A7↔#009E73 ΔE 7.6 (deutan) · tritan 10.7
  [PASS] Normal-vision floor    worst all-pairs #CC79A7↔#D55E00 ΔE 16.4 (normal)
  [PASS] Contrast vs surface    all 4 >= 3:1

  → ALL CHECKS PASS  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.


$ node scripts/validate_palette.js "#D55E00,#009E73,#6A3D9A,#CC79A7,#56B4E9" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 5 slots
  [PASS] Lightness band         all 5 inside L 0.43–0.77
  [PASS] Chroma floor           all 5 >= 0.1
  [WARN] CVD separation         worst all-pairs #CC79A7↔#009E73 ΔE 7.6 (deutan) · tritan 10.7
  [PASS] Normal-vision floor    worst all-pairs #CC79A7↔#D55E00 ΔE 16.4 (normal)
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#56B4E9",2.31]]

  → ALL CHECKS PASS  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.

================================================================================
RUN 10 (DOCUMENTS A BOUNDED EXCLUSION, expected FAIL) -- the six institution
hues AND the four OA domain hues as one ten-slot set. DISPOSITION: the
COEXISTENCE RULE (lib/palette.py module docstring, extended for 2B-1). A
Compare or Collaborate chart is coloured by INSTITUTION and by nothing else --
the categorical axis names the field / panel / goal / quadrant -- so the two
families are never in one figure and never in one legend. Recorded rather than
suppressed because the reader does scroll from domain-coloured Find panels to
institution-coloured Compare panels, i.e. the two families share SEQUENTIAL
memory; the fix if that ever becomes a real complaint is a different sixth
hue, never a second chart-dependent palette.

$ node scripts/validate_palette.js "#D55E00,#009E73,#6A3D9A,#CC79A7,#56B4E9,#E69F00,#0CA750,#FFCB3A,#8190FF,#F85C32" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 10 slots
  [FAIL] Lightness band         outside band: [["#FFCB3A",0.865]]
  [PASS] Chroma floor           all 10 >= 0.1
  [FAIL] CVD separation         worst all-pairs #F85C32↔#D55E00 ΔE 3.5 (protan) · tritan 1.7
  [FAIL] Normal-vision floor    worst all-pairs #0CA750↔#009E73 ΔE 6.0 (normal) — below 15, hard to tell apart even with full color vision
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#56B4E9",2.31],["#E69F00",2.25],["#FFCB3A",1.52],["#8190FF",2.85]]

  → FAILED — fix the marked checks  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.

================================================================================
RUN 11 (documents the CHROME distance, expected FAIL) -- the six institution
hues WITH FOCAL #0072B2 and COMPARISON #8C9196. The FAIL is entirely
COMPARISON's: chroma 0.009, and #8C9196 <-> #CC79A7 normal 12.2 / deutan 3.0.
That is the run-2 exclusion class restated (COMPARISON is a NEUTRAL, never an
identity), not a new finding, and it is exactly why COMPARISON is what
`institution_color` returns for an out-of-range slot -- an unassignable
institution must read as "no identity", which is what a low-chroma grey does.
FOCAL contributes NO failing pair. Its nearest institution hue is #6A3D9A at
normal-vision 15.6 / CVD 6.9 (matrix below) -- above the 15 normal-vision floor
and inside the 6-8 CVD band, which is legal here because FOCAL is never a MARK
on a Compare page: it paints Streamlit chrome (buttons, links, ProgressColumn
bars) and never appears in the same figure as an institution dot.

$ node scripts/validate_palette.js "#D55E00,#009E73,#6A3D9A,#CC79A7,#56B4E9,#E69F00,#0072B2,#8C9196" --mode light --surface "#FFFFFF" --pairs all

Palette (light, surface #FFFFFF, categorical): 8 slots
  [PASS] Lightness band         all 8 inside L 0.43–0.77
  [FAIL] Chroma floor           below floor (reads gray): [["#8C9196",0.009]]
  [FAIL] CVD separation         worst all-pairs #8C9196↔#CC79A7 ΔE 3.0 (deutan) · tritan 8.5
  [FAIL] Normal-vision floor    worst all-pairs #8C9196↔#CC79A7 ΔE 12.2 (normal) — below 15, hard to tell apart even with full color vision
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#56B4E9",2.31],["#E69F00",2.25]]

  → FAILED — fix the marked checks  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.

--- full pairwise matrix + distance to chrome (screen_inst.mjs) ---

pairwise (normal / worst-CVD):
  1-2  #D55E00 <-> #009E73   normal 25.8   cvd 11.0
  1-3  #D55E00 <-> #6A3D9A   normal 29.9   cvd 26.5
  1-4  #D55E00 <-> #CC79A7   normal 16.4   cvd 15.8
  1-5  #D55E00 <-> #56B4E9   normal 30.9   cvd 25.1
  1-6  #D55E00 <-> #E69F00   normal 15.6   cvd 13.1
  2-3  #009E73 <-> #6A3D9A   normal 30.4   cvd 22.4
  2-4  #009E73 <-> #CC79A7   normal 25.4   cvd 7.6
  2-5  #009E73 <-> #56B4E9   normal 18.4   cvd 16.6
  2-6  #009E73 <-> #E69F00   normal 24.2   cvd 11.4
  3-4  #6A3D9A <-> #CC79A7   normal 24.1   cvd 21.4
  3-5  #6A3D9A <-> #56B4E9   normal 31.2   cvd 27.5
  3-6  #6A3D9A <-> #E69F00   normal 40.5   cvd 38.6
  4-5  #CC79A7 <-> #56B4E9   normal 20.0   cvd 9.6
  4-6  #CC79A7 <-> #E69F00   normal 21.1   cvd 19.3
  5-6  #56B4E9 <-> #E69F00   normal 27.1   cvd 23.9
worst normal 15.6 (#D55E00<->#E69F00) | worst cvd 7.6 (#009E73<->#CC79A7)
contrast vs surface + distance to chrome:
  #D55E00 contrast 3.87  vsFOCAL normal 31.2 cvd 21.9  vsCOMPARISON normal 18.2 cvd 14.2
  #009E73 contrast 3.42  vsFOCAL normal 18.7 cvd 18.0  vsCOMPARISON normal 13.3 cvd 4.8
  #6A3D9A contrast 7.64  vsFOCAL normal 15.6 cvd 6.9  vsCOMPARISON normal 24.1 cvd 23.4
  #CC79A7 contrast 3.06  vsFOCAL normal 24.4 cvd 9.6  vsCOMPARISON normal 12.2 cvd 3.0
  #56B4E9 contrast 2.31  vsFOCAL normal 20.4 cvd 20.6  vsCOMPARISON normal 13.5 cvd 11.9
  #E69F00 contrast 2.25  vsFOCAL normal 36.2 cvd 29.2  vsCOMPARISON normal 19.4 cvd 16.5

================================================================================
RUN 12 -- GREY_STATE_COLORS, the five grey-accounting states of the coverage
strip. TWO runs, because the CATEGORICAL validator is not the applicable one:
these five steps are an ORDINAL severity scale (distance from usable text), so
they take a one-hue sequential ramp, and the validator's own scope line says
so -- "for a sequential ramp, lightness monotonicity". The categorical run is
printed first and FAILS BY DESIGN (five neutrals below the chroma floor;
adjacent normal-vision 7.8), the same exclusion class as runs 2 and 8. The
ORDINAL run (`validateOrdinal` from the same module) is the one that governs,
and it returns OK on all four of its checks.

The sixth segment of the strip -- classified-eligible -- is deliberately NOT in
this ramp: it is painted in the compared institution's own identity colour, so
the strip reads as highlight-plus-mute (one coloured segment that answers the
question, a muted ordered ramp accounting for the rest) and the only identity in
the figure is still the institution.

$ node scripts/validate_palette.js "#ACB2B9,#939AA2,#7B828A,#646B73,#4D535B" --mode light --surface "#FFFFFF" --pairs adjacent

Palette (light, surface #FFFFFF, categorical): 5 slots
  [PASS] Lightness band         all 5 inside L 0.43–0.77
  [FAIL] Chroma floor           below floor (reads gray): [["#ACB2B9",0.012],["#939AA2",0.014],["#7B828A",0.015],["#646B73",0.015],["#4D535B",0.015]]
  [WARN] CVD separation         worst adjacent #939AA2↔#ACB2B9 ΔE 7.7 (protan) · tritan 7.8
  [FAIL] Normal-vision floor    worst adjacent #939AA2↔#ACB2B9 ΔE 7.8 (normal) — below 15, hard to tell apart even with full color vision
  [WARN] Contrast vs surface    below 3:1 — relief required (visible labels or table view): [["#ACB2B9",2.14],["#939AA2",2.84]]

  → FAILED — fix the marked checks  (CVD in the 6–8 floor band is legal ONLY with secondary encoding: direct labels, gaps, or texture)
  scope: categorical palettes only. For a lone status/text color check WCAG text contrast; for a sequential ramp, lightness monotonicity.

--- the APPLICABLE validator for a sequential ramp (screen_ordinal.mjs) ---

=== #ACB2B9,#939AA2,#7B828A,#646B73,#4D535B  -> OK
    Lightness monotone | true | steps read light→dark
    Adjacent ΔL | true | all gaps >= 0.06
    Light-end contrast | true | #ACB2B9 at 2.14:1 vs surface
    Single hue | true | hue spread 4°
    contrasts: #ACB2B9 2.14  #939AA2 2.84  #7B828A 3.89  #646B73 5.39  #4D535B 7.77

