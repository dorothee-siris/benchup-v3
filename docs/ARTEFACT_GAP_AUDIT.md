# Artefact gap audit — DESIGN.md §4/§2.4 vs shipped `app/data/`

Verified 2026-08-29 against `app/data/*.parquet` (byte-identical to `V3/data/artefacts_eu/`,
confirmed by `cmp`) and `app/docs/data_contract.yaml`. One row per DESIGN.md expectation.
Status: **present** (shipped, no gap) / **derivable** (computable in-app from shipped data,
states how) / **GAP** (not present, not derivable from what's shipped). Every GAP carries a
named proposal — never silent (BUILD_PLAN L10 house rule).

| # | DESIGN.md expectation | Evidence (table.column) | Status | Note / proposal |
|---|---|---|---|---|
| 1 | Grey accounting **per institution × country × field** (DESIGN §4) | `index.mass_retracted_excluded`, `.mass_unusable`, `.mass_title_only`, `.mass_lang_uncertain`, `.mass_untranslated_grey`, `.mass_classified_eligible` | **CLOSED by ruling (2026-08-29)** | Shipped grain is per-institution ONLY (one row per institution, 6 mass columns) — no field breakdown (country is a 1:1 join via `index.country_code`, but field is not). **User ruling at gate 2A (BUILD_PLAN_2A.md §9 source line, L25): "grey disclosure institution-level is enough."** Grey accounting stays institution-level only through 2B; the `grey_by_field.parquet` proposal below is not being built. Gap closed, not deferred. |
| 2 | Frontier aggregates on the seed card (top-25% share, quadrant mix) | `index.frontier_top25_share`, `.frontier_quadrant_mix`, `.frontier_excluded_share`, `.frontier_unscored_share` | **present** | All 4 columns shipped, verified non-degenerate (frontier_quadrant_mix is a parseable `quadrant:share|...` string summing to ~1 over scored mass). |
| 3 | Subfield × year trend slices | `topics_all.vol_full_2020..2025` / `vol_frac_2020..2025` joined to `topics_dim.bestfit_subfield_id` (or original/conservative) | **derivable** | Sum the per-year columns over an institution's topics, grouped by the topic's subfield under the selected tree. Verified the per-year columns are dense (not string-packed) — a `groupby(subfield_id)[year_cols].sum()` per institution is a live, cheap query. **Field-level trajectories are NOT derivable**: `fields.parquet` and `pipeline/agg/derive.py`'s `fields_df` carry no by-year columns at all (derive.py docstring, "BY-YEAR FORMAT DEVIATION" — confirmed by construction, not just unshipped). Note this ceiling in the UI/methods copy rather than implying a field trend exists. |
| 4 | Impact bootstrap intervals (PP top10% + 95% CI) | `index.pp_top10_frac/.pp_ci_low/.pp_ci_high` (institution-level); `impact_cells.pp_top10_frac/.pp_ci_low/.pp_ci_high` (institution × subfield × tree × floor cells) | **present** | Both grains shipped; CI ordering (`ci_low <= pp <= ci_high`) verified on all rows of both tables (328,978 impact_cells rows, 7,557 index rows with non-null PP). `impact_cells` additionally ships all 3 trees × both floor variants (10 and 30) — wider than fields/subfields, which ship bestfit-only. |
| 5 | ERC classified-mass coverage bands per country | `erc.mass`/`.share` joined to `index.country_code` via `institution_id`; `index.erc_classified_mass_frac`/`erc_eligible_mass_frac` | **derivable** | Group `index` by `country_code`, aggregate `erc_classified_mass_frac` (numerator) and `erc_eligible_mass_frac` (denominator) → a per-country ERC coverage rate. No shipped file does this rollup; it is a two-column groupby, cheap live. |
| 6 | SDG classified/tagged-mass coverage bands per country | `index.sdg_classified_mass_frac`/`.sdg_eligible_mass_frac`/`.sdg_tagged_share` grouped by `country_code` | **derivable** | Same pattern as #5. Note the SDG-specific nuance: `sdg_tagged_share` (share of eligible mass that got >=1 keyword hit) is the more honest "coverage" figure than `sdg_classified_mass_frac` alone, since a work can be classified-attempted but tagged with 0 SDGs (DESIGN §5's "any-keyword-hit ⇒ 0–N SDGs/doc"). |
| 7 | Umbrella/aggregate badge, named-umbrella supplement | `data/overrides/umbrella_supplement.csv` (created this stream, 24 rows) | **created** | See `data_contract.yaml`'s `overrides/umbrella_supplement.csv` entry and `derived_views.umbrella_badge` for the union-with-automatic-rule logic. 3 of the 24 named umbrellas (AP-HP, Romanian Academy, Hungarian Research Network) carry a patched `type` outside the automatic rule's `{government,funder,facility,nonprofit}` gate — the supplement is the ONLY mechanism that flags them; disclosed in the CSV's own `note` column. |
| 8 | 811 catch-all exclusion list, versioned | `topics_dim.is_excluded` (811 True of 4,516), `.exclusion_reason_code` (811/811 non-null, 0/3,705 non-excluded have one — verified), `.exclusion_list_version` (single shipped value `'2026-08-21'`) | **present** | Count is COMPUTED (`topics_dim.is_excluded.sum()`), never a typed constant (L10). |
| 9 | Classifier accuracy figures (ERC recall by panel, SDG recall, taxonomy-repair recall) | none in `app/data/*.parquet` | **GAP (by design)** | These figures live only in `evals/` documents (e.g. `badge_calibration.md`, `judgments_v2_tabulation.md`, the taxonomy-repair audit) and are NOT computable from the shipped tables (no ground-truth/held-out columns are shipped). **Proposal:** a methods-note page in 2B that cites the eval documents directly (static prose + links, not a live-computed figure) — this is the one gap in this audit that is not a missing pipeline output but a deliberate non-goal of the app-data layer (evals stay in `evals/`, never ship to the app repo). No pipeline addition proposed; descope line for the gate list. |
| 10 | ESI-per-SDG vs European average (DESIGN §4, "unique-to-v3 indicator") | `sdg.esi`, `source_manifest.json.esi_europe_avg_sdg_share_classified_mass_basis` | **RESOLVED (2026-08-29)** | `sdg.esi` is shipped and internally consistent (deterministic rescaling of `sdg.share` per SDG, verified). The manifest's `esi_europe_avg_sdg_share_classified_mass_basis` vector is a DIFFERENT reference (classified-mass basis, not tagged-mass basis) from the one `esi` actually divides by (`pipeline/agg/erc_sdg_long.py::build_sdg_long`'s own `europe_avg = mass.sum(axis=0)/tagged_mass.sum()`, tagged-mass basis) — naive division by the manifest vector overstates a recomputed esi by a constant factor. VERIFIED NUMERICALLY (R-S5, 2026-08-29): SUM_index(sdg_classified_mass_frac) / SUM_index(sdg_tagged_share × sdg_eligible_mass_frac) = **4.2046** on `app/data/index.parquet`, matching the ~4.2046× offset flagged at gate 2A. `data_contract.yaml`'s `sdg.esi` entry now states esi's true (tagged-mass) denominator; no data change, no code change — a documentation reconciliation only. |

## Summary for the gate list
- **0 open GAPs.** #1 (grey accounting country×field) CLOSED by user ruling 2026-08-29 (institution-level only); #10 (sdg.esi vs the manifest vector) RESOLVED 2026-08-29 (documentation reconciliation, factor verified 4.2046).
- **1 by-design non-gap** (#9, classifier accuracy) — confirm the methods-note-in-2B plan rather than expecting a live figure.
- Everything else (#2–4, #6–8) is present or cheaply derivable with a named, verified how-to.
- **New in R1 (2026-08-29):** `doctype_by_year.parquet` (institution × year × doc_type volumes) ships as a new artefact, closing the "document type has no yearly breakdown" gap the profile redesign (BUILD_PLAN_2A.md L17) surfaced — see `data_contract.yaml`'s `doctype_by_year.parquet` entry.
